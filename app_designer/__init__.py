# This file makes the 'app_designer' directory a Python package.
# We can also expose key classes or functions here if desired for easier imports.
# For example:
# from .orchestrator import Orchestrator
# from .main import run_design_generation_app

# For now, keeping it simple.
# The agents are already handled by app_designer/agents/__init__.py
# The main.py is intended to be run as a script.
VERSION = "0.1.0"

print(f"app_designer package (version {VERSION}) loaded.")
